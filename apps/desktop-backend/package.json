{
  "name": "@helios/desktop-backend",
  "version": "0.8.0",
  "private": true,
  "description": "Shared Sealos and Agent sidecar for the Flutter desktop host.",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node24 --format=cjs --outfile=dist/helios-backend.cjs",
    "typecheck": "tsc --noEmit",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "@kubernetes/client-node": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.19.1",
    "esbuild": "^0.25.0",
    "typescript": "^5.9.3"
  }
}
