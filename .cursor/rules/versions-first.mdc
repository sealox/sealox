---
description: 新会话先读 versions/，再动手。vision 是目标，0.x 是已完成与当前版边界。
alwaysApply: true
---

# 新会话先读 versions

新会话第一条回复、提方案或改代码之前，先读 `versions/`。不要凭记忆、README 或只扫代码猜产品目标和完成度。

## 读什么

1. `versions/vision.md`：为什么存在、做/不做、两层结构、已定架构。
2. `versions/0.*.md`：按编号从低到高。文首写了「已完成」的是落地史；没写的是当前版。

文件是活文档，以磁盘为准，不要把内容背进别的规则。

## 读完要知道

- 产品边界：Helios 让 Sealos 更好用的桌面操作台，不是控制台翻版，也不是为做 agent 而做 agent。
- 已完成：不要重做已完成版本里的能力。
- 当前版：只做该文档写明的范围；「本版明确不做」不是待办。
- 架构已定项（Electron 主进程、本地 eve、不直连 Sealos）不要另开一条。

## 之后

同一会话不必每轮重读。`versions/` 有增删或当前任务改的是产品范围时再读。
