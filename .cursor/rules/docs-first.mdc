---
description: 新会话先读 docs/（当前功能 + versions 落地史），再动手。
alwaysApply: true
---

# 新会话先读 docs

新会话第一条回复、提方案或改代码之前，先读当前功能合同。不要凭记忆、根 README 或只扫代码猜产品目标和完成度。

## 读什么

1. `docs/README.md`：资源模型、两条做事路径、routing、执行规则。再按任务打开 `docs/references/` 里对应的一两篇。
2. `docs/versions/vision.md`：为什么存在、做/不做、已定架构。动产品边界或架构时必读。
3. `docs/versions/0.*.md`：按编号从低到高。文首写了「已完成」的是落地史；没写的是正在写的下一版范围。确认某能力是否已做、或要开新版本时再读。

文件是活文档，以磁盘为准，不要把内容背进别的规则。

## 读完要知道

- 产品边界：Helios 让 Sealos 更好用的桌面操作台，不是控制台翻版，也不是为做 agent 而做 agent。
- 当前功能以 `docs/README.md` 和 `docs/references/` 为准。`docs/versions/0.x` 是怎么走到这一步，不是「今天」的拼图。
- 新范围先写 `docs/versions/0.x.md`，再改代码。「本版明确不做」不是待办。
- 架构已定项（Electron 主进程、本地 eve、渲染进程不直连 Sealos）不要另开一条。

## 之后

同一会话不必每轮重读。`docs/` 有增删、或当前任务改的是产品范围时再读。
