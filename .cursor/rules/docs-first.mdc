---
description: 新会话先读 docs/index.md，再按任务打开那一两篇。
alwaysApply: true
---

# 新会话先读 docs

新会话第一条回复、提方案或改代码之前，先读当前功能合同。不要凭记忆、根 README 或只扫代码猜产品目标和完成度。

## 读什么

1. `docs/index.md`：产品是什么、资源模型、routing。再按任务打开对应那一两篇（`chat.md`、`templates.md` 等）。
2. `docs/versions/vision.md`：为什么存在、做/不做、已定架构。动产品边界或架构时才读。
3. `docs/versions/0.*.md`：确认某能力是否已做、或开新版本时才读对应那一篇。不要从 0.1 通读。

文件是活文档，以磁盘为准，不要把内容背进别的规则。

## 读完要知道

- 产品边界：在 Sealos 上做出自己的应用。Helios 是让 Sealos 更好用的桌面操作台。Agent 负责部署、照看、操作账号里的资源。不是控制台翻版，也不是为做 agent 而做 agent。
- 当前功能以 `docs/index.md` 和它链到的功能篇为准。`docs/versions/0.x` 是怎么走到这一步，不是「今天」的拼图。
- 新范围先写 `docs/versions/0.x.md`，再改代码。「本版明确不做」不是待办。
- 功能篇只写是什么和 happy path。Sealos API 对着源码查（`.cursor/rules/sealos-source.mdc`）。对话里怎么操作 Sealos 以 eve 的 use-sealos 为准。

## 之后

同一会话不必每轮重读。`docs/` 有增删、或当前任务改的是产品范围时再读。
