# iOS

第二个宿主。Expo，只做 iOS。

功能合同与桌面同一套（[index.md](index.md) 和各功能篇）。登录协议同一套，见 [auth.md](auth.md)。界面仍以 Figma 为准；稿上还没有 iOS 画面时，不要在代码里先长出来。

## 当前

仓库里有 `apps/mobile`。登录、对话、资源面都还没接到这个宿主。桌面仍是功能合同的实现。

## 不要

- Android、Expo Web
- 为 iOS 另写一套 Sealos 登录协议
- 在 Figma 没有对应稿时做产品画面
- 把未移植当成已经可用
